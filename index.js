const divide = function() {
  return 2000 / 100;
};

const square = x => Math.pow(x, 2);

const add = (x, y) => {
  return x + y;     // needs explicit return keyword if arrow-function body is more than 1 line
};